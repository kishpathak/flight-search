/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AdminService } from './admin.service';

describe('AdminService', () => {

  it('should be Truthy', () => {
    let app = new AdminService();
    expect(app).toBeTruthy();
  });
});
